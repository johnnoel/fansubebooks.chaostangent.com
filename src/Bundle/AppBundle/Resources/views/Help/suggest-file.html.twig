{% extends '::layout.html.twig' %}
{% set location = 'help' %}

{% block body %}
<article>
    <header class="page-header">
        <h2>Contribute a script</h2>
    </header>

    <div class="content">
        {% if form is not defined %}
        <div class="alert alert-success">Thanks for contributing a script! It might take a couple of days to make it into the library, but if you <a href="https://twitter.com/fansub_ebooks">tweet @fansub_ebooks</a> you can keep updated on progress.</div>
        <p>
            <a href="{{ path('help') }}" class="btn btn-lg">&larr; Back to help page</a>
            <a href="{{ path('home') }}" class="btn btn-lg btn-action">&larr; Back to homepage</a>
        </p>
        {% else %}
        <div class="alert alert-error">There was an error submitting your script contribution, any errors are displayed below</div>
        {% form_theme form 'ChaosTangentFansubEbooksAppBundle::form_layout.html.twig' %}
        {{ form_start(form) }}
        <fieldset>
            <legend>Upload a script</legend>
            {{ form_row(form.file) }}
            {{ form_row(form.upload) }}
        </fieldset>
        {{ form_end(form) }}
        {% endif %}
    </div>
{% endblock %}
